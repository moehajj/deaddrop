# Deaddrop

## Build

```sh
mkdir build
cd build
cmake ..
make
```

You will find the sender & receiver executables in the '/build/bin' directory.

## Run

### Parameters
### With default parameters
Sender:
```sh
./sender
```

Receiver:
```sh
./receiver
```
